<?php

use App\Model\User;

it('can create a contact', function () {
    // $user = Mockery::mock(User::class);
    // $user->shouldReceive('setName')->with('John Doe');
    // $user->shouldReceive('setEmail')->with('john@doe.com');
    // $user->shouldReceive('setPassword')->with('password');
    expect(true)->toBe(true);
});
